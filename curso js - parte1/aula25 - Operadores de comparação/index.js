/*Operadores de comparação

Utilizamos os:

  >  -> Maiore que
  >= -> Maior ou igual que
  <  -> Menor que
  <= -> Menor ou igual á
  == -> Igualdade (Verifica apenas valor) -------- Não é muito utilizado ---------------
 === -> Igualdade estrita (Verifica valor e tipo)
  != -> Diferente (Verifica apenas valor) -------- Não é muito utilizaod ---------------
 !== -> Difenrete estrito (Verificar valo e tipo)

 Nunca informaram um número, apenas informaram se é verdadeiro ou se é falso(Boolena).
*/ 
const num1 = 10
const num2 = 11;
const comp = num1 < num2;
console.log(comp);