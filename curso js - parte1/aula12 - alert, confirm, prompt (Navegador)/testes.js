// Comando alert() (Cria uma caixa com uma mensagem);

//alert('Com a nossa mensagem');

// Compando confirm() (Valores boleeanos de true or false);

//confirm("Deseja continuar para o site")

//Comando prompt() (Solicita que a pessoa digite algo);

//prompt("Digite o seu nome");

// Utilizado comanco em variáveis;

const confirma = confirm("Deseja continuar?");

const nome = prompt("Poderia informar o seu nome por gentileza?") // Pode ser utilizado para solicitar números;

let num1 = Number(prompt("Poderia informar o primeiro numero para ser calculado"));
let num2 = Number(prompt ("Poderia informar o segundo numero que deseja ser calculado"));

console.log((num1) + (num2));