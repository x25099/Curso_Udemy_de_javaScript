const nome = `Matheus`; // string
const nome2 = "Matheus"; // string
const nome3 = 'Matheus'; // string

const num = 1; // number
const num2 = 10.52; // number

let nomeAluno; // undefined -> não aponta pra lugar nenhum na memória
const sobrenomeAluno = null // Nulo -> Não aponta para nenhum lugar na memória

const boolean = true // ou (false) -> valores boleanos (lógico)